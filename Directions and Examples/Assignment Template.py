def functiontemplate(exampleparameter):
    """
    Description of the function goes here

    Parameters:
        exampleparameter: description

    Variables:
        examplevar (var type (float, int, etc)): variable description

    Logic:
        1. Step 1.
        2. Step 2.

    Return:
        Fill in if the function returns something. If not, "None"
    """
    # Make sure to still write above-line comments to annotate code
    examplevar = int(exampleparameter * 2)
    print(examplevar)